[package]
name = "ancre-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared domain types: pins, audit events, config snapshots."

[dependencies]
ancre-canon = { workspace = true }
serde = { workspace = true }
smallvec = { workspace = true }
uuid = { workspace = true }
time = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# The snapshot envelope crosses a process boundary as JSON, so the round trip
# is part of what the tests check.
serde_json = { workspace = true }

[features]
# Deterministic event fixtures for tests and benches. Never enabled by a
# binary — nothing under `fixtures` may end up in a shipped gateway.
fixtures = []

[lints]
workspace = true
